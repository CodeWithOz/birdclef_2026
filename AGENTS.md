## Learned User Preferences

- For Python tasks in this repo, use the existing `.venv` and `uv` instead of system `python` or `pip`.

## Learned Workspace Facts

- BirdCLEF 2026 local data has 234 taxonomy species, while `train.csv` has 206 unique `primary_label` species.
